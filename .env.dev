# Stripe Sandbox API Configuration - Dev Environment
# Non-sensitive configuration
BASE_URL=https://api.stripe.com

# Secrets are in .env.dev.secrets (gitignored)
# Create that file and add: STRIPE_SECRET_KEY=sk_test_your_key
